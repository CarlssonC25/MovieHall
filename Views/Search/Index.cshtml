@{
    ViewData["Title"] = "Search";
}

<h2>Suchergebnisse</h2>

@if (!string.IsNullOrEmpty(ViewBag.Query))
{
    <p>Du hast gesucht nach: <strong>@ViewBag.Query</strong> (@ViewBag.Type)</p>
}
else
{
    <p>Bitte gib einen Suchbegriff ein.</p>
}